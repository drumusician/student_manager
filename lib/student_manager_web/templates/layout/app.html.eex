<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>StudentManager · Manage your students with ease!</title>
    <link rel="stylesheet" href="<%=Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <nav class="navbar navbar-wrapper navbar-fade navbar-light is-transparent">
      <div class="container">
        <!-- Brand -->
        <div class="navbar-brand">
          <a class="navbar-item" href="/">
            <h1 class="is-size-3">STUDMAN</h1>
          </a>

          <%= if @conn.assigns.current_user && teacher?(@conn.assigns.current_user) do %>
          <a class="navbar-item" href="/students">My Students</a>
          <% end %>
          <!-- Responsive toggle -->
          <div class="custom-burger" data-target="">
            <a id="" class="responsive-btn" href="javascript:void(0);">
              <span class="menu-toggle">
                <span class="icon-box-toggle">
                  <span class="rotate">
                    <i class="icon-line-top"></i>
                    <i class="icon-line-center"></i>
                    <i class="icon-line-bottom"></i>
                  </span>
                </span>
              </span>
            </a>
          </div>
          <!-- /Responsive toggle -->
        </div>

        <!-- Navbar menu -->
        <div class="navbar-menu">
          <!-- Navbar Start -->
          <div class="navbar-start">
            <!-- Navbar item -->
            <a class="navbar-item is-slide" href="landing-v1-features.html">
              Features
            </a>
            <!-- Navbar item -->
            <a class="navbar-item is-slide" href="landing-v1-pricing.html">
              Pricing
            </a>
            <!-- Navbar item -->
            <a class="navbar-item is-slide" href="landing-v1-login.html">
              Login
            </a>
          </div>

          <!-- Navbar end -->
          <div class="navbar-end">
            <!-- Signup button -->
            <div class="navbar-item">
              <a id="#signup-btn" href="landing-v1-signup.html" class="button button-signup btn-outlined is-bold btn-align light-btn raised">
                Sign Up Now!
              </a>
            </div>
          </div>
        </div>
      </div>

      <div class="navbar-end">
        <div class="buttons">
          <%= if @conn.assigns.current_user do %>
          <%= link(@conn.assigns.current_user.email, to: "#", class: "navbar-item button is-light") %>
          <%= link "Sign out", to: Routes.pow_session_path(@conn, :delete), method: :delete, class: "navbar-item button is-primary" %>
          <% else %>
          <%= link "Sign in", to: Routes.pow_session_path(@conn, :new), class: "navbar-item button is-light" %>
          <%= link "Register", to: Routes.pow_registration_path(@conn, :new), class: "navbar-item button is-light" %>
          <% end %>
        </div>
      </div>
    </nav>
    <div class="section">
      <main role="main" class="container">
        <%= if get_flash(@conn, :info) do %>
        <div class="notification is-success"><button class="delete"></button><%= get_flash(@conn, :info) %></div>
        <% end %>
        <%= if get_flash(@conn, :error) do %>
        <div class="notification is-danger"><button class="delete"></button><%= get_flash(@conn, :error) %></div>
        <% end %>
        <%=render @view_module, @view_template, assigns %>
      </main>
    </div>
    <script type="text/javascript" src="<%=Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
